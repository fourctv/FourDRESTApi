  // ----------------------------------------------------  // users_GetUserGroups : Return list of Groups a given user sees  //  // Parameters:  //     $1: pointer to array to return user's group list  //     $2: user name  //  // Assumptions:  //  //  // ----------------------------------------------------C_POINTER($1;$groupListPtr)C_TEXT($2;$userName)  //--- copy parameters to locals$groupListPtr:=$1  //--- localsC_LONGINT($i)  //--- codeGET GROUP LIST($aGroups;$aIDs)  // get list of 4D user GroupsFor ($i;1;Size of array($aGroups))If (User in group($userName;$aGroups{$i}))  // if user is in this groupAPPEND TO ARRAY($groupListPtr->;$aGroups{$i})  // add it to the returned listEnd if End for SORT ARRAY($groupListPtr->)  // order the list, jic